dbt_mfg:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'manufacturing') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'manufacturing') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'manufacturing') }}"
      schema: analytics
      threads: 4
